# إصلاح TV Pro

المشكلة الأساسية ليست في شكل الصفحة، بل في طريقة وصول المتصفح للمصادر:

1. الموقع يعمل عبر HTTPS بينما كثير من اشتراكات IPTV تعطي روابط HTTP. طلبات fetch/XHR من صفحة HTTPS إلى HTTP تُحجب كـ Mixed Content.
2. قوائم M3U/HLS قد تأتي من نطاق مختلف بلا CORS. لذلك يجب تمرير القائمة وملفات HLS التابعة لها عبر HTTPS Gateway، وليس ملف m3u8 الرئيسي فقط.
3. Safari وChrome على iPhone لا يستطيعان تشغيل كل حاويات/ترميزات VOD. MKV/AVI وبعض الترميزات تحتاج نسخة MP4/H.264/AAC أو HLS متوافق، أو Transcoding حقيقي على الخادم. الـ Worker لا يقوم بتحويل الفيديو.

## التثبيت
- انشر `cloudflare-worker.js` كـ Cloudflare Worker.
- افتح رابط الـ Worker وتأكد أن الصفحة تعرض: `TV Pro Stream Gateway OK`.
- في TV Pro: الإعدادات > التشغيل > HTTPS proxy، ضع رابط الـ Worker فقط، مثال: `https://tvpro-gateway.<account>.workers.dev`.
- أعد إضافة القائمة/السيرفر بعد حفظ الإعداد.

## ملاحظة مهمة للأفلام والمسلسلات
إذا بقي فيلم بعينه لا يعمل بينما القنوات تعمل، افحص امتداد/Codec المصدر. الـ Worker يصلح HTTPS/CORS/HLS، لكنه لا يحول MKV/AVI أو Codec غير مدعوم إلى صيغة يدعمها iPhone. للحل الشامل يلزم Transcoding/Remux server-side (مثل FFmpeg على VPS)، وليس Cloudflare Worker.

## تحديث v4 (إصلاح القنوات والأفلام التي لا تعمل)
1. **المشغّل:** آخر رفع للموقع أعاد نسخة قديمة من المشغّل ضاع فيها إصلاح v3. أُعيد الإصلاح في `page-ff3b789c9f2b8c19.js`: عند خطأ Format/Decode ينتقل المشغّل إلى البوابة التالية بدل أن يتوقف. وحُدّث `sw.js` إلى `tvpro-shell-v6` حتى تصل النسخة الجديدة للمستخدمين.
2. **بوابة Cloudflare (`cloudflare-worker.js`):**
   - لا يستطيع Cloudflare الاتصال إلا بمنافذ محددة (80، 8080، 8880، 2052، 2082، 2086، 2095، 443، 2053، 2083، 2087، 2096، 8443). السيرفرات التي تعمل على منافذ مثل 25461 أو 8000 ترجع الآن خطأ 502 فورًا، فينتقل التطبيق إلى بوابة Deno التي لا تقيّدها المنافذ.
   - تتبّع الـ redirects يدويًا مع حفظ الرابط النهائي، وإعادة كتابة كل روابط `URI=` في القائمة.
   - إذا تحوّل رابط `.m3u8` إلى بث `.ts` مستمر فإنه يُمرَّر كما هو ولا يعلق.
   - إذا أرجع السيرفر صفحة HTML (اشتراك منتهٍ أو محجوب) بدل القائمة، يُرجَع خطأ 502 واضح.
   - دعم HEAD دون إرسال HEAD للسيرفر، لأن كثيرًا من السيرفرات ترفضه.
3. **بوابة Deno (`tvpro-gateway-deno.ts`):** نفس الإصلاحات، وأهمها أن البوابة كانت تعلق عندما يتحوّل رابط `.m3u8` إلى بث `.ts`.

### النشر (مطلوب لتفعيل إصلاح البوابات)
- Cloudflare: `npx wrangler deploy` (الملف `wrangler.toml` يشير إلى `cloudflare-worker.js`).
- Deno Deploy: الصق محتوى `tvpro-gateway-deno.ts` في مشروع `great-fox-5853` ثم اضغط Deploy.
- إذا كان `gateway.tv-pro.app` بوابة منفصلة، فحدّثها بأحد الملفين.
